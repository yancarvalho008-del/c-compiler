#include <stdio.h>

float tabela;
int tabela [3][3];
tabela[0][1];
tabela[1][2];
tabela[2][3];
tabela[3][4];

int main() {
    for (contador = 0; contador < 7; contador++) {
        printf("Digite os numeros da tabela %d: ", tabela + [3][3]);
        scanf(%f - %f,tabela);
    }
    return 0;
}

